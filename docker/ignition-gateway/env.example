## Ignition Gateway (Docker/Colima) example env
##
## Copy to .env (NOT committed) and adjust values:
##   cp docker/ignition-gateway/env.example docker/ignition-gateway/.env

# Image tag (choose an 8.1.x tag from inductiveautomation/ignition)
IGNITION_TAG=8.1.51

# Host port mappings (avoid conflicts with local installs)
HOST_HTTP_PORT=8097
HOST_HTTPS_PORT=8047

# Required by the official image entrypoint
ACCEPT_IGNITION_EULA=Y

# Toggle unsigned modules (dev flow). Set to false for "non-dev".
IGNITION_ALLOW_UNSIGNED_MODULES=true


